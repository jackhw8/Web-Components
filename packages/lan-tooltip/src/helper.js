/**
 * convertNumToInt is a helper function to convert
 * the parameter num to an int.
 * 
 * @param {string} num
 * 
 * @return {number} the int value of the corresponding param.
 */
export let convertNumToInt = num => {
  return num ? parseInt(num) : 0;
};